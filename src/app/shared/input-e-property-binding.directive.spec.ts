import { InputEPropertyBindingDirective } from './input-e-property-binding.directive';

describe('InputEPropertyBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new InputEPropertyBindingDirective();
    expect(directive).toBeTruthy();
  });
});
