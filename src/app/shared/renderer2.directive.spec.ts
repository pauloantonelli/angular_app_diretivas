import { Renderer2Directive } from './renderer2.directive';

describe('Renderer2Directive', () => {
  it('should create an instance', () => {
    const directive = new Renderer2Directive();
    expect(directive).toBeTruthy();
  });
});
