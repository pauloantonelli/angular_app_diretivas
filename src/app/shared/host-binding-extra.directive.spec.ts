import { HostBindingExtraDirective } from './host-binding-extra.directive';

describe('HostBindingExtraDirective', () => {
  it('should create an instance', () => {
    const directive = new HostBindingExtraDirective();
    expect(directive).toBeTruthy();
  });
});
